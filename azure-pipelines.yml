trigger:
  - main  # Trigger pipeline on changes to 'main' branch

pool:
  vmImage: 'ubuntu-latest'  # Temporarily use the hosted agent pool

steps:
  - script: echo "Hello, World!"  # A simple echo command to test the pipeline
    displayName: 'Echo Hello World'
