trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

steps:
  - task: Docker@2
    inputs:
      containerRegistry: 'Dockerproject'
      repository: 'Dockerproject'
      command: 'buildAndPush'
