trigger:
  - main  # Trigger pipeline on changes to 'main' branch

pool:
  name: 'SelfHostedAgents'  # Use your self-hosted agent pool
  demands:
    - Agent.Version -gtVersion 2.163.1  # You can try modifying this demand, or removing it

steps:
  - script: echo "Hello, World!"  # A simple echo command to test the pipeline
    displayName: 'Echo Hello World'
